---
driver:
  name: docker
  socket: <%= ENV['DOCKER_HOST'] %>

kitchen:
  test_base_path: spec/integration

provisioner:
  name: chef_solo

platforms:
  - name: ubuntu-12.04
  - name: centos-6.4

suites:
  - name: default
    run_list:
      - recipe[test]
